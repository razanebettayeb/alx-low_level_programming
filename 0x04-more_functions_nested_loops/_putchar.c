#include "main.h"
#include <unisted.h>
/**
 * _putchar.c - write the character c to stdout
 * @c: the character to print 
 *
 * Retutn: On success 1.
 * on error, -1 is returned , and errno is setappropriatly
 */
int _putchar.c(char c)
{
	return(write(1, &c,  1));
}
